STATUS
 M src/Haven.Desktop/Views/Shell/TopRail/UniversalSearchControl.axaml.cs
DIFF
diff --git a/src/Haven.Desktop/Views/Shell/TopRail/UniversalSearchControl.axaml.cs b/src/Haven.Desktop/Views/Shell/TopRail/UniversalSearchControl.axaml.cs
index b07f9cc..2a7044b 100644
--- a/src/Haven.Desktop/Views/Shell/TopRail/UniversalSearchControl.axaml.cs
+++ b/src/Haven.Desktop/Views/Shell/TopRail/UniversalSearchControl.axaml.cs
@@ -151,7 +151,7 @@ public sealed partial class UniversalSearchControl : UserControl
         });
         var detail = item.IsEnabled || string.IsNullOrWhiteSpace(item.DisabledReason)
             ? item.Detail
-            : $"{item.Detail} ├é┬À {item.DisabledReason}";
+            : $"{item.Detail} ┬À {item.DisabledReason}";
         var copy = new StackPanel
         {
             Spacing = 1,
@@ -172,7 +172,7 @@ public sealed partial class UniversalSearchControl : UserControl
                 Padding = new Thickness(10, 4),
                 Child = new TextBlock
                 {
-                    Text = item.IsEnabled ? item.KindLabel : $"{item.KindLabel} ├é┬À unavailable",
+                    Text = item.IsEnabled ? item.KindLabel : $"{item.KindLabel} ┬À unavailable",
                     FontSize = 10,
                     FontWeight = FontWeight.Bold
                 }
