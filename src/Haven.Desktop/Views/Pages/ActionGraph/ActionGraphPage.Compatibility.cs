namespace Haven.Desktop.Views.Pages.ActionGraph;

public sealed partial class ActionGraphPage
{
    private void AddDetailCard(string heading, string body)
        => AddDetailCard("NodeDetails", heading, body);
}
