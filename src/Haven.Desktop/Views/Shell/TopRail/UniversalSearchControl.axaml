<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:hui="using:Haven.Desktop.HavenUI.Components"
             xmlns:controls="using:Haven.Desktop.Controls"
             x:Class="Haven.Desktop.Views.Shell.TopRail.UniversalSearchControl">
  <StackPanel Spacing="12">
    <hui:HavenAdaptiveSurface Background="{DynamicResource HavenElevatedBrush}"
            BorderBrush="{DynamicResource HavenLineBrush}"
            BorderThickness="1"
            CornerRadius="22"
            Padding="5">
      <Grid>
        <hui:HavenTextInput x:Name="SearchBox"
                 AutomationProperties.AutomationId="LauncherSearchBox"
                 AutomationProperties.Name="Search Haven"
                 PlaceholderText="Search Haven"
                 Padding="43,13,14,13"
                 FontSize="15"
                 BorderThickness="0"
                 Background="Transparent"/>
        <controls:HavenIcon IconKey="search"
                            Width="20" Height="20"
                            Margin="14,0,0,0"
                            HorizontalAlignment="Left"
                            VerticalAlignment="Center"
                            IsHitTestVisible="False"/>
      </Grid>
    </hui:HavenAdaptiveSurface>

    <hui:HavenAdaptiveSurface Background="{DynamicResource HavenElevatedBrush}"
            BorderBrush="{DynamicResource HavenLineBrush}"
            BorderThickness="1"
            CornerRadius="24"
            Padding="15">
      <ScrollViewer MaxHeight="500"
                    HorizontalScrollBarVisibility="Disabled"
                    VerticalScrollBarVisibility="Auto">
        <StackPanel x:Name="ResultsPanel" Spacing="5"/>
      </ScrollViewer>
    </hui:HavenAdaptiveSurface>

    <Grid ColumnDefinitions="*,*" ColumnSpacing="8">
      <hui:HavenButton x:Name="ViewAllButton"
              AutomationProperties.AutomationId="LauncherCommandsButton"
              AutomationProperties.Name="All commands"
              Classes="sidebar"
              HorizontalAlignment="Stretch"
              HorizontalContentAlignment="Stretch"
              Padding="16,13"
              CornerRadius="18">
        <StackPanel Orientation="Horizontal" Spacing="10">
          <controls:HavenIcon IconKey="chevron-right" Width="18" Height="18"/>
          <TextBlock Text="All commands" FontWeight="ExtraBold" VerticalAlignment="Center"/>
        </StackPanel>
      </hui:HavenButton>
      <hui:HavenButton x:Name="SearchSettingsButton"
              AutomationProperties.AutomationId="SearchSettingsButton"
              AutomationProperties.Name="Search settings"
              Grid.Column="1"
              Classes="sidebar"
              HorizontalAlignment="Stretch"
              HorizontalContentAlignment="Stretch"
              Padding="16,13"
              CornerRadius="18">
        <StackPanel Orientation="Horizontal" Spacing="10">
          <controls:HavenIcon IconKey="settings" Width="18" Height="18"/>
          <TextBlock Text="Search settings" FontWeight="ExtraBold" VerticalAlignment="Center"/>
        </StackPanel>
      </hui:HavenButton>
    </Grid>
  </StackPanel>
</UserControl>
